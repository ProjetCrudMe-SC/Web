<table class="table">
    <thead>
    <tr>
        <th scope="col">ID</th>
        <th scope="col">Titre</th>
        <th scope="col">Date de Publication</th>
        <th scope="col">Qui a écrit ?</th>
        <th scope="col">Delete</th>
    </tr>
    </thead>
    <tbody>
    {% for article in articles %}
        <tr>
            <td>{{ article.Id }}<i class="bi bi-display"></i></td>
            <td><a href="/?controller=Article&action=show&param={{ article.id }}">{{ article.Titre }}</a></td>
            <td>{{ article.DatePublication|date("d/m/Y") }}</td>
            <td>{{ article.Auteur }}</td>
            <td><a href="/?controller=Article&action=delete&param={{ article.id }}">X-Delete-X</a></td>
        </tr>
    {% endfor %}
    </tbody>
</table>